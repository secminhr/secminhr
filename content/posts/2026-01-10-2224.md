---
title: "2026-01-10 2224"
date: 2026-01-10T22:24:25+0800
categories: ["Draft"]
tags: ["PLT"]
---

最近在讀 A Syntactic Approach to Type Soundness (A.K. Wright and M. Felleisen., 1994) 時，他們的文章中使用 reduction semantics 來描述語意。因為我對這套使用 context 的描述法不熟，所以在這裡想要記錄一下。

# Syntax
他們使用的語言 syntax 如下：
$$
e ::= v\ |\ e_1 e_2\ |\ \text{let } x \text{ be } e_1 \text{ in } e_2\ \ \ \ \text{(Expressions)}\\
v ::= c\ |\ x\ |\ \text{Y}\ |\ \lambda x.e\ \ \ \ \text{(Values)}
$$

其中 $c$ 是一些額外定義的 constant，$x$ 是變數，$\text{Y}$ 是弄出 recursion 用的 fixpoint combinator。

# Semantics
他們首先定義了 $\to$ relation 來表示某一個單位的單步 reduction。
$$
c\ v \to \delta(c, v)\\
(\lambda x.e) v \to e[x \mapsto v] \\
\text{let } x \text{ be } v \text{ in } e \to e[x \mapsto v] \\
\text{Y}v \to v(\lambda x. (\text{Y} v) x)
$$

$\delta(c, v)$ 只是為了處理 $c$ 不知道是什麼（或者說那些額外定義的常數）的情況用的。

