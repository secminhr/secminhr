baseURL: "https://secminhr.github.io/secminhr/" # 如果你的 Repo 名稱跟帳號一樣，請改成 "https://secminhr.github.io/"
languageCode: "zh-tw"
title: "Drafts"
theme: "PaperMod"

enableRobotsTXT: true
buildDrafts: false
buildFuture: true

# Markdown 渲染設定
markup:
  highlight:
    codeFences: true
    guessSyntax: true
    lineNos: true
    style: monokai # 程式碼配色
  goldmark:
    renderer:
      unsafe: true # 允許在 markdown 中使用 HTML
    extensions:
      passthrough:
        enable: true
        delimiters:
          block:
            - - $$
              - $$
            - - \[
              - \]
          inline:
            - - $
              - $
            - - \(
              - \)

# 網站參數
params:
  env: production
  title: "Drafts"
  description: "Drafts, Articles, and Random Thoughts."
  author: "secminhr"
  defaultTheme: auto # 自動切換深色模式
  mainSections:
    - posts 
  # 功能開關
  math: true # 數學公式支援
  ShowReadingTime: true
  ShowPostNavLinks: true
  ShowBreadCrumbs: true
  ShowCodeCopyButtons: true
  
  # 首頁簡介區塊
  homeInfoParams:
    Title: "Hi 👋"
    Content: "這裡是用來堆放草稿、進度與想法的地方。"

  # 社交連結
  socialIcons:
    - name: github
      url: "https://github.com/secminhr"

# 選單設定
menu:
  main:
    # 1. 精選 (Featured) - 權重 10
    - identifier: featured
      name: ✨ 精選
      url: /tags/featured/
      weight: 10
    
    # 2. 文章 (Articles) - 權重 20
    - identifier: articles
      name: 文章
      url: /categories/article/
      weight: 20
      
    # 3. 草稿 (Drafts) - 權重 30
    - identifier: drafts
      name: 草稿
      url: /categories/draft/
      weight: 30

    # 4. 歸檔 (Archives) - 權重 40
    - identifier: archives
      name: 歸檔
      url: /archives/
      weight: 40

    # 5. 標籤 (Tags) - 權重 50
    - identifier: tags
      name: 標籤
      url: /tags/
      weight: 50
      
    # 6. 搜尋 (Search) - 權重 60
    - identifier: search
      name: 搜尋
      url: /search/
      weight: 60

# 輸出格式 (搜尋功能必需)
outputs:
  home:
    - HTML
    - RSS
    - JSON